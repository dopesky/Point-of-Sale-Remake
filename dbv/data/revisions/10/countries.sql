ALTER TABLE `countries` ADD `create_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `currency_code`, ADD `modified_date` TIMESTAMP on update CURRENT_TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `create_date`, ADD `suspended` BOOLEAN NOT NULL DEFAULT FALSE AFTER `modified_date`;